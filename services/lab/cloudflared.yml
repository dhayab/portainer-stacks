services:
  cloudflared:
    x-target: ridge
    container_name: lab-cloudflared
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token $CLOUDFLARED_TOKEN_RIDGE

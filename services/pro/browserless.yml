services:
  browserless:
    x-target: ridge
    container_name: pro-browserless
    image: browserless/chrome:latest
    restart: unless-stopped
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
      TOKEN: $BROWSERLESS_TOKEN
    ports:
      - 3330:3000
